#include <bits/stdc++.h>
using namespace std;

int main() {
	int t;
	cin>>t;
	while(t--)
	{
	   int a[7];
	   for(int i=0;i<6;i++)
	   {
	       cin>>a[i];
	   }
	   int p = a[5];
	   int count=0;
	   for(int i=0;i<5;i++)
	   {
	       count +=a[i]*p;
	   }
	   if(count<=120)
	   {
	       cout<<"No"<<endl;
	   }
	   else
	   {
	   cout<<"Yes"<<endl;
	   }
	}
	return 0;
}
